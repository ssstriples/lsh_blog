# 📋 기술 블로그 (lsh_blog) 요구사항 명세서

> 작성일: 2026-08-07  
> 참고 프로젝트: `onggi-shop` (Next.js + Express + Prisma 기반 쇼핑몰) 기술 스택 재활용

---

## 1. 프로젝트 개요

- **목적**: 개인 기술 블로그 개발 및 운영
- **베이스**: `onggi-shop`에서 검증된 기술 스택/아키텍처를 최대한 재사용
  - 인증(로그인/회원), 페이지 라우팅 구조, 게시글(구 "상품") CRUD, 관리자 페이지 등을 블로그 도메인에 맞게 변형
- **차별점**: AI를 활용해 "요구조건 명세서"와 "상세 기술 명세서"를 자동/반자동으로 생성하고 축적하는 것을 목표로 함

---

## 2. 핵심 방향 2가지

### 방향 1 — onggi-shop 기술 Base 재사용

| onggi-shop 개념 | lsh_blog 매핑 | 비고 |
|---|---|---|
| 회원(User) / 로그인(JWT+NextAuth) | 동일 — 관리자(작성자) 계정 1개 + 방문자(선택) | 소셜 로그인은 방문자 댓글용으로 축소 가능 |
| 상품(Product) CRUD | 게시글(Post) CRUD | TipTap 리치 에디터, 이미지(Cloudinary), 소프트 삭제 그대로 활용 |
| 카테고리(Category, self-ref) | 블로그 카테고리/태그 | 트리 구조 → 카테고리, N:M → 태그 |
| 리뷰(Review) | 댓글(Comment) | 별점 제외, 텍스트 + 대댓글 |
| 커뮤니티/공지(Post, Question) | Q&A, 방명록 (선택 기능) | 우선순위 낮음 |
| 관리자 대시보드/통계 | 조회수·인기글 통계 대시보드 | Recharts 재사용 |
| 검색(Search) | 게시글 제목/본문 검색 | 동일 |
| CI/CD, 보안 미들웨어(Helmet, RateLimit, JWT 이중 시크릿) | 동일 그대로 적용 | 재사용 |

### 방향 2 — AI 기반 명세서 작성 파이프라인

블로그 자체를 "AI로 명세서를 만드는 과정"의 결과물로 축적한다.

1. **요구조건 명세서(개략)**: 아이디어 → 사용자 스토리 → 대략적 기능 목록 (AI 초안 생성 → 사람 검수)
2. **상세 기술 명세서**: 기능별 API, DB 스키마, 컴포넌트 설계, 시퀀스 다이어그램까지 AI로 상세화
3. 이 두 문서를 블로그 글 포맷(게시글)으로 발행 → "AI 명세서 작성법" 자체가 블로그 콘텐츠가 됨

---

## 3. 기술 스택 (onggi-shop 기준 재사용)

### Frontend
- Next.js 16 (App Router), React 19
- TailwindCSS 4, shadcn/ui, Radix UI
- TanStack Query (서버 상태), Zustand (클라이언트 상태 — 좋아요/임시저장 등)
- TipTap (에디터), react-hook-form + zod (폼 검증)
- next-auth v5 (JWT 전략)

### Backend
- Node.js + Express v5, TypeScript
- Prisma v7 (PostgreSQL, driver adapter)
- bcryptjs, jsonwebtoken (Access/Refresh 이중 시크릿)
- helmet, express-rate-limit, cors (보안)
- Cloudinary (이미지 업로드), winston (로깅)

### 인프라
- 로컬 개발 IP: `10.159.78.35` (참고용 — DB 접속/CORS 화이트리스트 설정 시 사용)
- 배포: Vercel(FE) + Railway/기타(BE) — onggi-shop 배포 가이드(`06_cicd_deploy.md`) 참고

---

## 4. 예상 폴더 구조 (onggi-shop 참고)

```
lsh_blog/
├── frontend/                 ← Next.js App Router
│   ├── src/app/
│   │   ├── (main)/
│   │   │   ├── posts/[slug]/         ← 게시글 상세
│   │   │   ├── category/[slug]/      ← 카테고리별 목록
│   │   │   ├── search/
│   │   │   └── about/
│   │   ├── (auth)/login/
│   │   └── admin/
│   │       ├── posts/ (new, [id]/edit)
│   │       ├── comments/
│   │       └── stats/
│   └── src/components/{layout,post,comment,admin,common}/
├── backend/
│   ├── prisma/schema.prisma  ← User, Post, Category, Tag, Comment, PostView ...
│   └── src/{controllers,services,routes,middlewares,schemas,lib}/
└── docs/
    ├── 00_index.md
    ├── 01_requirements_draft.md     ← AI 생성 요구조건 명세서
    ├── 02_tech_spec_detail.md       ← AI 생성 상세 기술 명세서
    ├── 03_db_schema_erd.md
    ├── 04_api_endpoints.md
    └── 05_ai_writing_pipeline.md    ← AI 명세서 자동화 파이프라인 문서
```

---

## 5. 진행 단계 (Phase)

| Phase | 내용 | 상태 |
|---|---|---|
| Phase 0 | 프로젝트 초기 세팅 (onggi-shop 보안/설정 이식) | 🔲 예정 |
| Phase 1 | DB 스키마 설계 (User/Post/Category/Tag/Comment) | 🔲 예정 |
| Phase 2 | 인증 시스템 (관리자 로그인, 필요 시 소셜 로그인) | 🔲 예정 |
| Phase 3 | 게시글 CRUD (에디터, 이미지 업로드, 카테고리/태그) | 🔲 예정 |
| Phase 4 | 댓글 시스템 | 🔲 예정 |
| Phase 5 | 검색 & 통계 대시보드 | 🔲 예정 |
| Phase 6 | AI 명세서 자동 생성 파이프라인 구축 | 🔲 예정 |
| Phase 7 | 배포 & CI/CD | 🔲 예정 |

---

## 6. 다음 액션

1. `docs/` 폴더 생성 및 `01_requirements_draft.md`(개략 요구조건), `02_tech_spec_detail.md`(상세 기술명세) 템플릿 작성
2. onggi-shop `schema.prisma`를 참고하여 블로그용 스키마 초안 작성 (User, Post, Category, Tag, Comment, PostView)
3. onggi-shop 인증 미들웨어/보안 설정 코드 복사 후 블로그 도메인에 맞게 축소
4. AI 명세서 생성 프롬프트 템플릿 설계 (요구조건 → 기술명세 2단계 체인)
