# 백엔드 API 서버 주소 (Express, 기본 포트 4100)
NEXT_PUBLIC_API_URL=http://localhost:4100

# NextAuth 설정 (Phase 2-3 프론트엔드 인증 구현 시 사용 — T039)
# openssl rand -base64 32 등으로 랜덤 값 생성 권장
# NEXTAUTH_SECRET=
# NEXTAUTH_URL=http://localhost:3000
